name: Docker Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          submodules: false

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build Docker images
        run: |
          for dir in CI; do   # 👈 Solo carpetas que tienen Dockerfile
            echo "🔨 Construyendo imagen en $dir ..."
            tag="test-$(echo $dir | tr '[:upper:]' '[:lower:]')"
            docker build -t $tag $dir || exit 1
          done

      - name: Run container tests
        run: |
          for dir in CI; do   # 👈 Solo test para carpetas válidas
            tag="test-$(echo $dir | tr '[:upper:]' '[:lower:]')"
            echo "🚀 Ejecutando contenedor de $tag ..."
            docker run --rm $tag echo "✅ Imagen $dir funciona correctamente"
          done



