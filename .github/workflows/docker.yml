name: Docker Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout del repositorio
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. Configurar Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # 3. Construir imágenes de cada carpeta
      - name: Build Docker images
        run: |
          for dir in CI Cache Capas Dependencies Distroless Ignore MultiStage Prune Scripts Usuarios dockerscan; do
            if [ -f "$dir/Dockerfile" ]; then
              img_name=$(echo "$dir" | tr '[:upper:]' '[:lower:]')
              echo "🔨 Construyendo imagen en $dir como test-$img_name ..."
              docker build -t test-$img_name $dir || exit 1
            else
              echo "⚠️ No se encontró Dockerfile en $dir, saltando..."
            fi
          done

      # 4. Ejecutar pruebas sobre los contenedores
      - name: Run container tests
        run: |
          for dir in CI Cache Capas Dependencies Distroless Ignore MultiStage Prune Scripts Usuarios dockerscan; do
            if [ -f "$dir/Dockerfile" ]; then
              img_name=$(echo "$dir" | tr '[:upper:]' '[:lower:]')
              echo "🚀 Ejecutando contenedor test-$img_name ..."
              docker run --rm test-$img_name echo "✅ Imagen $dir funciona correctamente"
            fi
          done

