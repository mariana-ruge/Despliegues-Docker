name: Docker Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout del repositorio
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. Configurar Docker Buildx (para builds más avanzados)
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # 3. Construir imágenes de cada carpeta que contenga un Dockerfile
      - name: Build Docker images
        run: |
          for dir in CI Cache Capas Dependencies Distroless Ignore MultiStage Prune Scripts Usuarios dockerscan; do
            echo "🔨 Construyendo imagen en $dir ..."
            docker build -t test-$dir $dir || exit 1
          done

      # 4. Probar que los contenedores corren
      - name: Run container tests
        run: |
          for dir in CI Cache Capas Dependencies Distroless Ignore MultiStage Prune Scripts Usuarios dockerscan; do
            echo "🚀 Ejecutando contenedor de test-$dir ..."
            docker run --rm test-$dir echo "✅ Imagen $dir funciona correctamente"
          done
